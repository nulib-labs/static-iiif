version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci --prefix ui
    build:
      commands:
        - npm run build --prefix ui
  artifacts:
    baseDirectory: ui/dist
    files:
      - '**/*'
  cache:
    paths:
      - ui/node_modules/**/*
